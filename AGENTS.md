# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal Git test repository (`GitTestPastuhh`) containing only `README.md` and `Hello.txt`. There are:

- **No application code** — no source files in any programming language
- **No dependency manifests** — no `package.json`, `requirements.txt`, `pyproject.toml`, etc.
- **No build system** — no `Makefile`, CI/CD configs, or build scripts
- **No tests** — no test frameworks or test files
- **No services** — no databases, servers, or external dependencies

There is nothing to build, lint, test, or run beyond standard Git operations. The update script is a no-op (`true`).

If application code is added in the future, update this file and the VM update script accordingly.
